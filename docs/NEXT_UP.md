# TrueSystems Next Up

Status: current human-visible execution order, not capability authority

This file exposes the immediate work sequence. No item becomes implemented,
authorized, or qualified because it appears here. Code plus passing external
tests remain capability truth.

## 1. Resolve the system naming scheme

Resolve the owner-facing and code-facing names currently written as `TrueCog`,
`CompuCog`, and related lineage names before the final repository/runtime seal.
Do not perform a bulk rename from prose. Inventory package names, imports,
entrypoints, manifests, worker identities, schemas, receipts, paths, help, and
governing documents; then present the witnessed collisions and a proposed final
name map to the owner for approval.

Until that decision is made, existing implemented identifiers remain unchanged.
No alias gains authority merely because a document uses it.

## 2. Add self-describing bounded-operation correction packets

Specify and qualify the deterministic operation-requirements response described
in [Bounded Operation Requirements and Protection Plan](BOUNDED_OPERATION_REQUIREMENTS_AND_PROTECTION_PLAN.md).
An authorized caller must be able to recover from missing, misnamed, unknown,
or invalid caller-supplied arguments without relying on model memory. The host
must never disclose or delegate host-owned authority inputs as caller fields.

This is an interface capability to implement and test. Current generic
`INVALID_ARGUMENTS` errors do not satisfy it.

## 3. Begin the Neo4j third-party graph-control addition

The canonical owner-supplied design input remains unchanged at:

`/home/lamercey/Downloads/Neo4j_Third_Party_Graph_Control_Addition_Plan.docx`

SHA-256:
`badb384c051b196c25fe7ee1f4fc06de96d2e2d3796ee2076327dc57d71861a6`

The document's original sequence gate placed Neo4j after the final integrity
seal. The owner's 2026-09-14 instruction explicitly supersedes that ordering:
Neo4j graph control is NEXT UP, human-visible work; the final seal occurs only
after integration, reconciliation, consolidation, qualification, and the naming
decision are complete.

The Neo4j boundary remains unchanged:

- TrueSystems artifacts are authoritative; Neo4j is a disposable derived view.
- Initial graph operations are read-only with respect to protected code,
  datasets, manifests, receipts, policy, and authority state.
- Neo4j receives projections and returns graph locations/relationships; it does
  not mutate canonical inputs or become evidence authority.
- Agent access is only through registered TrueCore operations.
- Credentials, roots, grants, executable bindings, budgets, and resource
  assignments remain host-owned.
- Every operation must expose the self-describing correction contract before it
  is made available to a model or agent.

## 4. Reconcile and qualify the connected system

After the immediate additions:

1. Graph the actual connected machine and retain missing authority explicitly.
2. Reconcile governing help and documentation against code and passing tests.
3. Remove duplicate paths, dead workers, temporary adapters, stale manifests,
   conflicting schemas, unused edges, and abandoned experiments only after
   their custody and replacement are proven.
4. Consolidate durable receipts at the bounded-run level while retaining
   incident receipts for meaningful failures or security events. Debug telemetry
   is not permanent capability evidence.
5. Classify artifacts as runtime-critical, authority/governance, supporting
   configuration, governing documentation, derived output, or historical
   archaeology.
6. Qualify connected success, denial, missing prerequisites, partial failure,
   corrected retry, and recovery.

## 5. Final repository and runtime protection seal

This is deliberately last. The protected 2026-09-14 snapshot is a restorable
checkpoint, not a declaration that the changing repository is finally sealed.

The final protection phase must implement and qualify the placeholders in the
protection plan: secured mutation routing, smallest-provable-region drift
localization, safe/secure mode, suspect-state preservation, quarantine,
independently verified clean recovery, requalification, and attribution only as
far as witnessed OS/runtime evidence permits.

