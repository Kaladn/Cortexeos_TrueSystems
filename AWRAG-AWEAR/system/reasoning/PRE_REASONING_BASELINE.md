# Pre-Reasoning Baseline

Baseline commit:

```text
54fdb38 Add optional adapter auto-selection lane
```

All commits up through `54fdb38` are treated as pre-reasoning work.

That baseline includes:

```text
core evidence mechanics
adapter boundary cleanup
metadata isolation from core batch loading
pressure-probe sidecar
generic query/batch evidence packet proof
off-path packet inspection notes
```

## Baseline Lesson

The baseline tests AW as:

```text
question retrieval
-> qualification
-> speech
```

Any later thinking work must remain outside this path:

```text
question
-> claim formation
-> evidence verification
-> cited support
-> claim verification
```

## Next Lane

Future work starts after this baseline and should be recorded as reasoning-lane work before any promotion into core behavior.
