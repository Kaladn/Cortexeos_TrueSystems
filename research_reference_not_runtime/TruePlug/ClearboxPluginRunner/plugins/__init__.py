"""Forest AI plugins directory.

All plugins live here: lakespeak/, wolf_engine/, etc.
Entry points add this directory to sys.path so internal imports
(e.g. `from lakespeak.xxx`) resolve without rewriting.
"""
