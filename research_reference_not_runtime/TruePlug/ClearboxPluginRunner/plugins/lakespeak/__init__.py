"""LakeSpeak v0 — Retrieval-augmented grounding plugin for Forest AI.

Provides grounded inference: query -> retrieve -> rerank -> cite -> respond.
No heavy deps at import time — all external libraries lazy-loaded on first use.
"""

VERSION = "0.1.0"
